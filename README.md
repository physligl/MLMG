# MLMG

LaTeX source repository for 《并行多重网格方法：数学原理、并行算法与异构实现》.

Current release: **v0.10**.

This source package tracks source files only. Generated PDFs and LaTeX build artifacts are intentionally excluded.

## Compile

```bash
latexmk -xelatex main.tex
```
