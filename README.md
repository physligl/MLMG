# MLMG

LaTeX source repository for 《并行多重网格方法：数学原理、并行算法与异构实现》.

Current release: **v0.10.1**.

The source tree tracks manuscript sources only. Generated PDFs are published as GitHub Release assets; LaTeX build artifacts remain excluded.

## Compile

```bash
latexmk -xelatex main.tex
```
